{
  "scripts": {
    "build": "NODE_ENV=prod rollup -c",
    "dev": "NODE_ENV=dev rollup -c -w",
    "clear": "rm -rf dev"
  },
  "dependencies": {
    "@rollup/plugin-replace": "^4.0.0",
    "rollup-plugin-monkey": "github:wdssmq/rollup-plugin-monkey#v1"
  }
}
